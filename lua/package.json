{
  "name": "lua-browser-repl",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:lua": "node build-libs.js",
    "build": "npm run build:lua",
    "serve": "npx serve"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
